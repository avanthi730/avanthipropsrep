name: Create Properties File

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  create-properties-file:
    runs-on: ubuntu-latest
    environment: Dev

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Create properties file
        run: |
          echo "DB_HOST=${{ vars.DB_DATABASE_HOST }}" >> dev.properties
          echo "DB_PORT=${{ vars.DB_DATABASE_PORT }}" >> dev.properties
          echo "DB_USER=${{ secrets.DB_DATABASE_USER }}" >> dev.properties
          echo "DB_PASS=${{ secrets.DB_DATABASE_PASSWORD }}" >> dev.properties

      - name: Upload properties file
        uses: actions/upload-artifact@v3
        with:
          name: dev-properties
          path: dev.properties
